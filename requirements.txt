# --- Core (TUI + OpenAI backend) ---
rich>=13.0
openai>=1.0
python-dotenv>=1.0
PyYAML>=6.0

# --- Optional: local models via Ollama ---
# ollama>=0.3

# --- Optional [verify]: robust port extraction for cross-validation ---
# pyverilog>=1.3

# --- External tools (not pip-installable) ---
# Icarus Verilog (iverilog, vvp) must be on PATH:
#   Debian/Ubuntu : sudo apt install iverilog
#   macOS (brew)  : brew install icarus-verilog
